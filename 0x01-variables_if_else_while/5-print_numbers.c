#include <stdio.h>
/**
 * main - Runs from start to finish
 *
 * Description: Prints single digit no's
 * Return: Always return 0
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
